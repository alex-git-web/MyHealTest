/* Medical archive (active panels) */

const base_url = "./img/";
const localStorageActivePanel = `${base_url}/local_storage_panel_image.png`;

export const images = {
  localStorageActivePanel: localStorageActivePanel,
};
